# 8 Adımlı Güncelleme Paketi — 2026-06-06

Bu paket, proje içindeki eksik dokümanların tamamlanması, temel UI placeholder'larının eklenmesi ve linter/dokümantasyon hatalarının giderilmesi için izlenecek adımları içerir.

1. Kuruluş bilgilerini ve iletişimleri güncelle

- Aşağıdaki dosyaların içeriğini doğrulayın:

  - `gizlilik-politikasi.md`

  - `guvenlik-politikasi.md`

  - `kvkk-aydinlatma.md`

  - `hizmet-sartlari.md`

1. Markdown lint kontrolü çalıştır

- Önerilen: `npx markdownlint-cli2 "**/*.md"`

- Hedef: MD032, MD012, MD029, MD007, MD034 hatalarını giderin.

1. Eksik UI sayfalarını ekle

- `index.html`, `styles.css`, `app.js` (kök dizin)

- `setup_ui/index.html`

1. Ana README ve CHANGELOG güncelle

- `README.md` projenin kısa özeti ve hızlı başlangıç yönergelerini içermeli.

- `CHANGELOG.md` güncellemesi yapılmalı.

1. Katkı ve güvenlik rehberleri ekle

- `CONTRIBUTING.md` ve `SECURITY.md` dosyalarını kontrol edin.

1. Otomasyon betiğini çalıştır ve doğrula

- `scripts/apply_update.ps1` isteğe bağlı commit adımlarını içerir.

- Betiği test edin (önce `-WhatIf` veya `-Confirm:$false` olmadan çalıştırmayın).

1. Commit ve tag

- Değişiklikleri commit edin:

```powershell
git add -A
git commit -m "chore(update): 2026-06-06 — doküman ve UI placeholder'ları eklendi"
git tag -a v0.1.0 -m "Update package 2026-06-06"
```

1. Yayın ve bildirim

- Değişiklikleri pushlayın ve ilgili ekip/ilgili kişilere bildirin.

```powershell
git push origin main --follow-tags
```

Notlar

- Her adımı uygularken linter ve testleri çalıştırın. İstediğiniz takdirde tüm adımları otomatikleştiren PowerShell betiğini çalıştırabilirim.
